name: "Base PRP Template v3 - Implementation-Focused with Precision Standards"
description: |

---

## Goal

**Feature Goal**: [Specific, measurable end state of what needs to be built]

**Deliverable**: [Concrete artifact - API endpoint, service class, integration, etc.]

**Success Definition**: [How you'll know this is complete and working]

## 📸 Visual Reference & Demo Analysis Requirements

**Demo/Reference Analysis (MANDATORY for all visual projects)**:

**1. Visual Reference Source:**
- **Reference Type**: `[Website URL / Image file / Figma design / Video demo / Live application]`
- **Reference Location**: `[Provide complete URL, file path, or description]`
- **Reference Screenshots**: `[Required - capture key screens/sections]`

**2. Comprehensive Visual Dissection (Phoenix Orchestration Required):**
```yaml
demo_analysis_process:
  step_1_capture:
    - [ ] Full-page screenshot of reference
    - [ ] Individual section screenshots (header, hero, content, footer)
    - [ ] Mobile/responsive view captures  
    - [ ] Interactive element states (hover, active, focus)
    - [ ] Animation/transition documentation

  step_2_measurement:
    - [ ] Layout dimensions and spacing measurements
    - [ ] Color palette extraction (hex codes)
    - [ ] Typography analysis (fonts, sizes, weights, line heights)
    - [ ] Component sizing and proportions
    - [ ] Breakpoint behavior analysis

  step_3_technical_analysis:
    - [ ] CSS framework identification (Bootstrap, Tailwind, custom)
    - [ ] Component structure breakdown
    - [ ] Layout method analysis (Grid, Flexbox, positioning)
    - [ ] Asset requirements identification
    - [ ] Animation/interaction patterns documentation
```

**3. Reference Fidelity Requirements:**
- **Pixel-Perfect Match**: `[Required / Close approximation / Inspired by / Custom interpretation]`
- **Responsive Behavior**: `[Match exactly / Adapt patterns / Custom approach]`
- **Interactive Elements**: `[Replicate functionality / Similar behavior / Enhanced version]`
- **Performance Standards**: `[Match or exceed reference performance metrics]`

**4. CRITICAL: Playwright MCP Validation Requirements (MANDATORY for all visual projects):**

**🎭 PLAYWRIGHT MCP VALIDATION PROTOCOL (AGENTS MUST FOLLOW)**:
```yaml
playwright_validation_workflow:
  during_implementation:
    - [ ] CAPTURE: Use `mcp__playwright__browser_take_screenshot` to capture current implementation
    - [ ] COMPARE: Visual comparison between reference and current implementation
    - [ ] VALIDATE: Layout structure, spacing, and component positioning match reference
    - [ ] CHECK: Typography rendering, font sizes, and text hierarchy alignment
    - [ ] VERIFY: Color accuracy, contrast ratios, and visual consistency
    - [ ] TEST: Responsive behavior across breakpoints (mobile, tablet, desktop)
    
  validation_checkpoints:
    - After completing each major component
    - Before marking any visual task as completed
    - When implementing responsive design changes
    - After typography and animation system setup
    - Before final PRP validation sign-off
    
  playwright_mcp_commands:
    screenshot_capture: "mcp__playwright__browser_take_screenshot"
    browser_resize: "mcp__playwright__browser_resize"
    element_interaction: "mcp__playwright__browser_click"
    navigation: "mcp__playwright__browser_navigate"
    
  quality_gates:
    - [ ] Reference fidelity matches requirements (pixel-perfect, close, inspired)
    - [ ] No visual deviations without explicit approval
    - [ ] Responsive behavior matches or exceeds reference standards
    - [ ] All interactive elements function as specified
    - [ ] Performance meets or exceeds reference benchmarks
```

**AGENTS INSTRUCTION: You MUST use Playwright MCP server throughout implementation to ensure alignment with visual references. Never implement visual features "blindly" - always validate against the reference using screenshot comparisons.**

**5. Demo Analysis Documentation:**
```yaml
# Visual Reference Analysis Report (Auto-generated by agents)
reference_analysis:
  visual_hierarchy:
    - Primary focal points: [List main visual elements]
    - Secondary elements: [Supporting content and navigation]
    - Visual flow: [How eyes move through the design]
  
  layout_structure:
    - Grid system: [12-column, 16-column, custom, CSS Grid]
    - Container widths: [max-width values, breakpoints]
    - Spacing system: [margin/padding patterns, rhythm]
  
  design_tokens:
    colors:
      primary: "#[hex-code]"
      secondary: "#[hex-code]"
      accent: "#[hex-code]"
      neutral: "#[hex-code]"
    typography:
      headings: "[font-family, sizes, weights]"
      body: "[font-family, sizes, line-heights]"
    spacing:
      base_unit: "[4px, 8px, 1rem, etc.]"
      scale: "[multiples and rhythm]"
```

**5. Implementation Strategy from Reference:**
- **Replication Approach**: `[Component-by-component / Section-by-section / Full recreation]`  
- **Asset Extraction**: `[Use reference assets / Generate similar / Create custom]`
- **Code Strategy**: `[Reverse-engineer CSS / Use similar framework / Custom implementation]`

## 🎬 GSAP Animation & Typography System Requirements

### **GSAP Animation System Integration** (Professional Motion Design)

**1. Animation Tier Assessment:**
```yaml
animation_requirements:
  gsap_implementation_tier: [Essential | Advanced | Performance | Creative]
  
  essential_tier: # 90% of projects - Basic GSAP patterns
    - [ ] Component hover states and micro-interactions
    - [ ] Page entrance animations (fade-in, slide-in)
    - [ ] Form validation feedback animations
    - [ ] Button press effects and loading states
    
  advanced_tier: # Complex UI interactions
    - [ ] ScrollTrigger-based reveal animations
    - [ ] Timeline-based sequence animations
    - [ ] Interactive drag and drop animations
    - [ ] Modal and drawer transition effects
    
  performance_tier: # High-traffic applications
    - [ ] 60fps animation consistency validation
    - [ ] Custom animation loops with gsap.ticker
    - [ ] Memory management and cleanup protocols
    - [ ] Animation performance budgets
    
  creative_tier: # Award-winning experiences
    - [ ] Complex timeline orchestration
    - [ ] Physics-based animations
    - [ ] Custom easing functions
    - [ ] Multi-element choreography
```

**2. GSAP Implementation Strategy:**
```yaml
gsap_technical_requirements:
  core_implementation:
    - [ ] GSAP library integration (CDN or npm install)
    - [ ] Plugin selection: ScrollTrigger, Draggable, TextPlugin
    - [ ] Framework integration (React useGSAP, Vue composables)
    - [ ] SSR compatibility for Next.js/Nuxt applications
  
  animation_patterns:
    basic_animations:
      - [ ] gsap.to() for property changes
      - [ ] gsap.from() for entrance effects  
      - [ ] gsap.fromTo() for complete control
      - [ ] gsap.set() for initial states
    
    timeline_management:
      - [ ] gsap.timeline() for sequence control
      - [ ] Timeline labels and callbacks
      - [ ] Stagger animations for multiple elements
      - [ ] Timeline scrubbing and control
    
    scroll_integration:
      - [ ] ScrollTrigger setup and configuration
      - [ ] Scroll-based animation triggers
      - [ ] Parallax scrolling effects
      - [ ] Progress-based animations
  
  accessibility_requirements:
    - [ ] prefers-reduced-motion media query support
    - [ ] gsap.matchMedia() for responsive animations
    - [ ] Animation pause/play controls
    - [ ] Screen reader compatibility
```

**3. GSAP Agent Coordination Workflow:**
```yaml
agent_gsap_responsibilities:
  prism_frontend_designer:
    - Query GSAP Master MCP for component-level animations
    - Focus: "GSAP micro-interactions", "GSAP hover effects"
    - Implement component animation libraries
  
  nextjs_ui_developer:
    - Query GSAP Master MCP for Next.js integration patterns
    - Focus: "GSAP Next.js SSR", "GSAP App Router compatibility"
    - Handle client-side animation initialization
  
  composition_maestro:
    - Query GSAP Master MCP for visual storytelling
    - Focus: "GSAP timeline orchestration", "GSAP visual narrative"
    - Design complex animation sequences
  
  velocity_flutter_dev:
    - Reference GSAP timing for Flutter equivalent animations
    - Focus: Cross-platform animation consistency
    - Translate GSAP patterns to Flutter animations
```

**4. GSAP Performance & Validation Requirements:**
```yaml
animation_validation:
  performance_metrics:
    - [ ] 60fps consistency across target devices
    - [ ] Animation bundle size < 50kb
    - [ ] First animation < 100ms after page load
    - [ ] Memory usage monitoring and cleanup
  
  quality_standards:
    - [ ] Smooth easing curves (no linear animations)
    - [ ] Appropriate duration timing (200-800ms for UI)
    - [ ] Consistent animation language across components
    - [ ] Progressive enhancement for older browsers
  
  testing_protocols:
    - [ ] Animation performance profiling
    - [ ] Cross-browser compatibility testing
    - [ ] Mobile performance validation
    - [ ] Accessibility compliance verification
```

## 📚 Typography System Requirements

**Animation & Typography Questions (MANDATORY for Professional Polish)**:

**1. Typography System Definition:**
- **Font Family Strategy**: `[Google Fonts / Custom fonts / System fonts / Web-safe fonts]`
- **Primary Font**: `[Font name, weights available: 300,400,500,600,700,900]`
- **Secondary Font**: `[Font name for headings/accents, weights available]`
- **Fallback Strategy**: `[System fallbacks for loading/offline states]`
- **Typography Scale**: `[Modular scale ratio: 1.125, 1.25, 1.333, 1.5, custom]`

**2. Typography Implementation Standards:**
```yaml
typography_system:
  font_definitions:
    primary_font: "[Inter, Roboto, custom]"
    weights: "[300, 400, 500, 600, 700, 900]"
    secondary_font: "[Playfair Display, Montserrat, custom]"
    
  type_scale:
    h1: "[font-size, line-height, font-weight, letter-spacing]"
    h2: "[font-size, line-height, font-weight, letter-spacing]" 
    h3: "[font-size, line-height, font-weight, letter-spacing]"
    h4: "[font-size, line-height, font-weight, letter-spacing]"
    body_large: "[font-size, line-height, font-weight]"
    body_regular: "[font-size, line-height, font-weight]"
    body_small: "[font-size, line-height, font-weight]"
    caption: "[font-size, line-height, font-weight]"
    
  responsive_typography:
    mobile: "[scale factor or specific sizes]"
    tablet: "[scale factor or specific sizes]" 
    desktop: "[scale factor or specific sizes]"
```

**3. Animation System Definition:**
- **Animation Philosophy**: `[Subtle/Moderate/Bold / Disney-inspired / Material Design / Custom]`
- **Transition Duration**: `[Fast: 150ms / Standard: 300ms / Slow: 500ms / Custom timing]`
- **Easing Functions**: `[ease-in-out / cubic-bezier custom / Material Motion / Custom curves]`
- **Animation Library**: `[Framer Motion / GSAP / CSS animations / Platform native / Custom]`

**4. Animation Implementation Standards:**
```yaml
animation_system:
  timing_standards:
    micro_interactions: "[100-200ms for hover, focus, active states]"
    component_transitions: "[200-300ms for modals, dropdowns, slides]"
    page_transitions: "[300-500ms for route changes, loading states]"
    complex_sequences: "[500ms+ for multi-step animations]"
    
  easing_curves:
    standard: "[cubic-bezier(0.4, 0.0, 0.2, 1) - Material Design standard]"
    decelerate: "[cubic-bezier(0.0, 0.0, 0.2, 1) - entering animations]"
    accelerate: "[cubic-bezier(0.4, 0.0, 1, 1) - exiting animations]"
    bounce: "[cubic-bezier(0.68, -0.55, 0.265, 1.55) - playful interactions]"
    
  animation_patterns:
    hover_effects: "[scale, shadow, color, transform specifications]"
    loading_states: "[skeleton, spinner, progressive, custom patterns]"
    micro_interactions: "[button press, input focus, toggle, checkbox]"
    page_enters: "[fade, slide, scale, custom entrance animations]"
    error_states: "[shake, flash, highlight, validation feedback]"
```

**5. Platform-Specific Animation & Typography Requirements:**

**For Python/Backend Projects:**
- **Typography**: Focus on API documentation, error messages, logging
- **Animation**: Progress indicators, loading states, real-time updates
- **Standards**: Consistent response formatting, error message styling

**For Next.js Projects:**  
- **Typography**: CSS-in-JS definitions, responsive scaling, web font optimization
- **Animation**: Framer Motion integration, CSS transitions, page transitions
- **Standards**: Core Web Vitals optimization, FOUT/FOIT prevention

**For Flutter Projects:**
- **Typography**: TextTheme system, Material/Cupertino typography scales
- **Animation**: Flutter's Animation framework, implicit/explicit animations  
- **Standards**: 60fps performance, platform-appropriate motion design

## 🌐 Multilingual Requirements

**Required Languages**: 
- ✅ English (Primary)
- ✅ French (Required)

**Optional Languages** (implement as needed):
- 🔄 German
- 🔄 Portuguese  
- 🔄 Spanish
- 🔄 Mandarin Chinese

**Localization Strategy**:
- [ ] i18n framework integration from project start
- [ ] Text extraction and string externalization
- [ ] Language detection and switching mechanism
- [ ] RTL support consideration (if Arabic/Hebrew planned)
- [ ] Date/time/number formatting localization
- [ ] Asset localization (images with text)

**Implementation Notes**:
- All user-facing text must be externalized from Day 1
- Database schema must support multilingual content
- API responses must include language context
- Frontend components must be i18n-ready
- Error messages and notifications must be translatable

## 🗄️ Database Architecture & Authentication System Requirements

**Database Architecture Questions (MANDATORY for all projects)**:

**1. Is a database required for this project?**
- [ ] Yes - User data, content management, or persistent storage needed
- [ ] No - Static functionality or file-based storage sufficient
- [ ] Hybrid - Some features need database, others don't

**2. What type of database system?**
- [ ] Relational (PostgreSQL, MySQL, SQLite) - Complex relationships and ACID compliance
- [ ] NoSQL (MongoDB, Firestore) - Flexible schema and rapid development
- [ ] Graph Database (Neo4j) - Complex relationship mapping
- [ ] Time-series (InfluxDB) - Analytics and monitoring data
- [ ] Key-Value Store (Redis) - Caching and session management

**3. Authentication & Authorization Requirements:**
```yaml
authentication_system:
  authentication_methods:
    - [ ] Email/Password authentication (required baseline)
    - [ ] OAuth integration (Google, Facebook, GitHub, Apple)
    - [ ] Two-factor authentication (2FA) security
    - [ ] Magic link/passwordless authentication
    - [ ] SSO integration (SAML, OIDC for enterprise)
    - [ ] Biometric authentication (mobile apps)
  
  user_roles_hierarchy:
    - [ ] Super Administrator (system-level access)
    - [ ] Administrator (organization-level management)
    - [ ] Manager/Owner (team/resource management)
    - [ ] Standard User (basic application access)
    - [ ] Guest/Viewer (read-only access)
    - [ ] Custom roles (define specific permissions)
  
  authorization_patterns:
    - [ ] Role-Based Access Control (RBAC)
    - [ ] Attribute-Based Access Control (ABAC)
    - [ ] Resource-based permissions
    - [ ] Multi-tenant access control
    - [ ] API key/token-based authorization
```

**4. Database Schema Architecture:**
```yaml
core_database_structure:
  user_management_tables:
    users:
      - id (UUID primary key)
      - email (unique, non-null)
      - hashed_password
      - first_name, last_name
      - created_at, updated_at
      - email_verified_at
      - status (active, inactive, suspended)
      - locale (for multilingual support)
    
    user_roles:
      - id (UUID primary key)  
      - user_id (foreign key)
      - role_name (admin, user, manager, etc.)
      - granted_at, granted_by
      - expires_at (optional)
    
    user_permissions:
      - id (UUID primary key)
      - user_id (foreign key)
      - resource_type, resource_id
      - permission_level (read, write, delete, admin)
      - granted_at, granted_by
    
    user_sessions:
      - id (UUID primary key)
      - user_id (foreign key)
      - session_token (hashed)
      - ip_address, user_agent
      - created_at, expires_at
      - is_active
  
  application_tables:
    organizations:
      - id (UUID primary key)
      - name, slug (unique)
      - owner_user_id (foreign key)
      - created_at, updated_at
      - settings (JSONB for flexible config)
    
    organization_members:
      - id (UUID primary key)
      - organization_id (foreign key)
      - user_id (foreign key)
      - role (admin, member, viewer)
      - joined_at, invited_by
```

**5. Database Security & Performance Requirements:**
```yaml
security_requirements:
  - [ ] Data encryption at rest and in transit
  - [ ] Password hashing with bcrypt/argon2
  - [ ] SQL injection prevention (parameterized queries)
  - [ ] Rate limiting for authentication endpoints
  - [ ] Audit logging for user actions
  - [ ] GDPR/privacy compliance features
  - [ ] Regular security updates and patches
  
performance_requirements:
  - [ ] Database indexing strategy for queries
  - [ ] Connection pooling configuration
  - [ ] Read replica setup for scaling
  - [ ] Caching strategy (Redis/Memcached)
  - [ ] Database backup and recovery plan
  - [ ] Migration strategy for schema changes
```

**6. Cross-Platform Authentication Integration:**
```yaml
platform_integration:
  nextjs_web:
    - [ ] NextAuth.js integration with JWT/session cookies
    - [ ] Middleware for route protection
    - [ ] Server actions for authentication flows
    - [ ] OAuth provider configuration
  
  flutter_mobile:
    - [ ] Firebase Authentication integration
    - [ ] Secure token storage (flutter_secure_storage)
    - [ ] Biometric authentication (local_auth)
    - [ ] Platform-specific OAuth flows
  
  python_backend:
    - [ ] FastAPI/Django authentication endpoints
    - [ ] JWT token generation and validation
    - [ ] Database ORM integration (SQLAlchemy/Django ORM)
    - [ ] Password reset and email verification flows
```

**Database Implementation Strategy**:
- Authentication system must be implemented first in any project requiring user management
- Database schema must be designed to support all identified user roles and permissions
- All platforms (web, mobile, backend) must share the same authentication service
- Database must be designed with multilingual content support from Day 1
- User management workflows must be consistent across all platforms

## 💳 Payment System & E-commerce Requirements

**Payment System Questions (MANDATORY for all commercial projects)**:

**1. Does this project require payment processing?**
- [ ] Yes - E-commerce, subscriptions, services, or transactions needed
- [ ] No - Free service, internal tool, or information-only project
- [ ] Future - Not immediately required but infrastructure should support it

**2. What type of payment model?**
- [ ] One-time payments (product sales, services)
- [ ] Subscription-based (monthly, yearly recurring billing)
- [ ] Freemium with premium features
- [ ] Pay-per-use/consumption-based billing
- [ ] Marketplace with vendor payouts
- [ ] Donation/tip-based payments

**3. Payment System Architecture:**
```yaml
payment_infrastructure:
  payment_processors:
    - [ ] Stripe (recommended for full-stack integration)
    - [ ] PayPal (broad user acceptance)
    - [ ] Square (for physical/online integration)
    - [ ] Razorpay (for Indian markets)
    - [ ] Custom payment gateway integration

  supported_payment_methods:
    - [ ] Credit/Debit Cards (Visa, Mastercard, Amex)
    - [ ] Digital wallets (Apple Pay, Google Pay, PayPal)
    - [ ] Bank transfers and direct debits
    - [ ] Buy now, pay later (Klarna, Afterpay)
    - [ ] Cryptocurrency payments (if applicable)
    - [ ] Local payment methods by region

  checkout_system:
    - [ ] Embedded checkout (hosted on your domain)
    - [ ] Redirect checkout (third-party hosted)
    - [ ] Mobile-optimized checkout flow
    - [ ] Guest checkout vs required registration
    - [ ] Multi-step vs single-page checkout
    - [ ] Shopping cart functionality with persistence
```

**4. E-commerce Database Schema Integration:**
```yaml
payment_database_structure:
  products_services:
    products:
      - id (UUID primary key)
      - name, description, sku
      - price, currency
      - stock_quantity, is_active
      - category_id, created_at, updated_at
      
    product_variants:
      - id (UUID primary key)
      - product_id (foreign key)
      - variant_name (size, color, etc.)
      - price_adjustment, stock_quantity
      
  orders_transactions:
    orders:
      - id (UUID primary key)
      - user_id (foreign key)
      - order_number (unique)
      - subtotal, tax_amount, shipping_cost, total_amount
      - currency, status (pending, paid, shipped, completed, cancelled)
      - shipping_address, billing_address
      - created_at, updated_at
      
    order_items:
      - id (UUID primary key)
      - order_id (foreign key)
      - product_id, product_variant_id
      - quantity, unit_price, total_price
      
    payments:
      - id (UUID primary key)
      - order_id (foreign key)
      - payment_intent_id (Stripe/payment processor ID)
      - amount, currency, status
      - payment_method_type
      - processed_at, created_at
      
  subscriptions:
    subscription_plans:
      - id (UUID primary key)
      - name, description
      - price, billing_interval (monthly, yearly)
      - features (JSONB for flexible plan features)
      - is_active, created_at
      
    user_subscriptions:
      - id (UUID primary key)
      - user_id (foreign key)
      - plan_id (foreign key)
      - stripe_subscription_id
      - status (active, cancelled, past_due)
      - current_period_start, current_period_end
      - created_at, cancelled_at
```

**5. Cross-Platform Payment Integration:**
```yaml
platform_payment_integration:
  nextjs_web:
    - [ ] Stripe Elements integration for secure card collection
    - [ ] Server Actions for payment processing
    - [ ] Webhook handling for payment status updates
    - [ ] Protected checkout routes with authentication
    - [ ] Shopping cart state management
    
  flutter_mobile:
    - [ ] Stripe Flutter SDK integration
    - [ ] Apple Pay and Google Pay native integration
    - [ ] In-app purchase support (for digital products)
    - [ ] Secure payment form validation
    - [ ] Offline payment queue for poor connectivity
    
  python_backend:
    - [ ] Stripe/payment processor API integration
    - [ ] Webhook endpoints for payment notifications
    - [ ] Order management and fulfillment logic
    - [ ] Subscription lifecycle management
    - [ ] Payment retry logic and dunning management
```

**6. Payment Security & Compliance:**
```yaml
security_compliance:
  - [ ] PCI DSS compliance (never store card data directly)
  - [ ] SSL/TLS encryption for all payment pages
  - [ ] 3D Secure (SCA) compliance for European customers
  - [ ] Fraud prevention and risk assessment
  - [ ] Payment data encryption and tokenization
  - [ ] Regular security audits and updates
  - [ ] GDPR compliance for payment data handling
```

**Payment Implementation Strategy**:
- Payment system infrastructure must be planned before UI development begins
- Database schema must include all payment, order, and subscription tables from Day 1
- Authentication system must integrate with payment user accounts
- All platforms must use the same payment service for consistent experience
- Shopping cart functionality must persist across user sessions and devices
- Payment flows must be optimized for mobile and desktop checkout

## 🎨 Template & Design System Requirements

**Template/Design Framework Questions (Must Answer Before Development)**:

**1. Existing Template/Design System?**
- [ ] Using shadcn/ui component library? (Specify components needed)
- [ ] Using TailwindCSS template? (Provide template name/source)
- [ ] Using custom design system? (Provide guidelines/documentation)
- [ ] Starting from scratch? (Justify why not using existing systems)

**2. Template Structure & Assets:**
- **Template Folder Path**: `[/path/to/template/folder]`
- **Template Type**: `[shadcn/ui / TailwindCSS / Custom / Figma Design / None]`
- **Style Framework**: `[TailwindCSS / CSS Modules / Styled Components / Emotion]`
- **Component Library**: `[shadcn/ui / Material-UI / Ant Design / Custom / None]`

**3. Image & Asset Strategy:**
- **Asset Source**: `[Existing template assets / Custom generation needed / Stock images / Mixed]`
- **Image Requirements**: `[Hero images / Product photos / Icons / Illustrations / Avatars]`
- **Generation Method**: `[AI generation / Designer provided / Stock photos / None needed]`
- **Image Formats**: `[SVG / PNG / JPEG / WebP / AVIF]`

**4. Layout Component Decisions:**
- **Header Style**: `[Fixed / Sticky / Static / Carousel / None]`
- **Hero Section**: `[Static / Carousel / Slider / Video / Interactive / None]`
- **Navigation**: `[Top nav / Side nav / Mega menu / Mobile drawer / Breadcrumbs]`
- **Footer**: `[Simple / Multi-column / Sticky / Newsletter signup / Social links]`

**5. Template-Specific Implementation Notes:**
```yaml
# Template Configuration
template_rules:
  - Follow existing: [color palette / typography / spacing / components]
  - Extend with: [custom components / additional pages / new sections]
  - Avoid: [breaking existing patterns / inconsistent styling / duplicate components]

# Development Strategy
development_approach:
  pre_development:
    - [ ] Template analysis and component inventory
    - [ ] Asset requirement specification
    - [ ] Design system documentation review
    - [ ] Component modification/extension planning
  
  during_development:
    - [ ] Follow template's file structure
    - [ ] Use template's utility classes and patterns
    - [ ] Extend components rather than recreating
    - [ ] Maintain design consistency
```

## User Persona (if applicable)

**Target User**: [Specific user type - developer, end user, admin, etc.]

**Use Case**: [Primary scenario when this feature will be used]

**User Journey**: [Step-by-step flow of how user interacts with this feature]

**Pain Points Addressed**: [Specific user frustrations this feature solves]

## Why

- [Business value and user impact]
- [Integration with existing features]
- [Problems this solves and for whom]

## What

[User-visible behavior and technical requirements]

### Success Criteria

- [ ] [Specific measurable outcomes]

## Recommended Skills for This PRP

> **Reference**: See `skills-ecosystem/prp-skill-maps/SKILL-PRP-MAPPING.md` for full mapping details.
> **Design Insights**: See `skills-ecosystem/catalog/DESIGN-INSIGHTS-GEMINI3-ERA.md` for creative best practices.

```yaml
# Skills to activate based on project type (select applicable categories)
skills_context:
  design_ui: # For visual/UI projects
    required:
      - ui-ux-pro-max        # 50 styles, 21 palettes, full design intelligence
      - frontend-design       # Production-grade, non-generic design
      - web-design-guidelines # Accessibility, UX compliance
    recommended:
      - tailwind-design-system     # Design tokens, Tailwind v4
      - tailwindcss-advanced-layouts # Complex Grid/Flexbox patterns

  typography: # For projects with text/content focus
    required:
      - typography            # Type scales, hierarchy, spacing
      - typography-expert     # Font pairing, variable fonts, OpenType
    recommended:
      - web-typography             # Responsive type, loading optimization
      - brand-typography-systems   # Brand identity, modular scale
      - font-pairing-suggester    # Google Fonts alternatives

  icons_visuals: # For icon and visual asset needs
    required:
      - icon-design           # Semantic icon selection (Lucide, Heroicons, Phosphor)
      - better-icons          # CLI search 200+ icon libraries
    recommended:
      - svg-icon-generator    # Custom SVG generation
      - favicon-gen           # Favicon set generation
      - logo-creator          # Logo with AI image gen

  react_nextjs: # For React/Next.js implementations
    required:
      - react-best-practices  # Hooks, effects, refs, patterns
    recommended:
      - nextjs-supabase-auth  # Auth integration
      - react-components      # Design-to-code conversion
      - ai-elements           # AI chat interface components

  backend: # For API/backend implementations
    required:
      - python-pro            # Python 3.14+ type safety, async, pytest
      - nodejs-backend-patterns # Express/Fastify, middleware, auth
    recommended:
      - better-auth-best-practices # Auth framework
      - mcp-builder           # MCP server creation

  ai_generation: # For AI image/video content
    recommended:
      - nano-banana-pro       # Gemini 3 Pro image gen/edit
      - flux-best-practices   # FLUX model prompting
      - midjourney-replicate-flux # Cinematic image prompts
      - image-upscaling       # Image enhancement
      - seedream-image        # ByteDance image gen

  content: # For copywriting and content
    recommended:
      - human-writing         # Natural, non-AI copy
      - social-content        # Social media strategies
      - seo-audit             # SEO optimization

  design_anti_patterns: # ALWAYS include for visual projects
    avoid:
      - "Generic purple/blue gradient backgrounds"
      - "Over-rounded corners on everything"
      - "Cookie-cutter SaaS template aesthetics"
      - "Broken icon placeholders (declare icon library upfront)"
      - "Backgrounds competing with foreground content"
    prefer:
      - "Reference-driven prompting (screenshots as wireframes)"
      - "Style steering ('in the style of Apple/Stripe/Linear')"
      - "Icon system declaration upfront (iconify + SVG logos)"
      - "Big typography with strong hierarchy"
      - "Monochrome + accent color strategy"
      - "Animate on scroll with consistent keyframes"
      - "Test at 3 breakpoints (desktop, tablet, mobile)"
```

## All Needed Context

### Context Completeness Check

_Before writing this PRP, validate: "If someone knew nothing about this codebase, would they have everything needed to implement this successfully?"_

### Documentation & References

```yaml
# MUST READ - Include these in your context window
- url: [Complete URL with section anchor]
  why: [Specific methods/concepts needed for implementation]
  critical: [Key insights that prevent common implementation errors]

- file: [exact/path/to/pattern/file.py]
  why: [Specific pattern to follow - class structure, error handling, etc.]
  pattern: [Brief description of what pattern to extract]
  gotcha: [Known constraints or limitations to avoid]

- docfile: [PRPs/ai_docs/domain_specific.md]
  why: [Custom documentation for complex library/integration patterns]
  section: [Specific section if document is large]
```

### Current Codebase tree (run `tree` in the root of the project) to get an overview of the codebase

```bash

```

### Desired Codebase tree with files to be added and responsibility of file

```bash

```

### Known Gotchas of our codebase & Library Quirks

```python
# CRITICAL: [Library name] requires [specific setup]
# Example: FastAPI requires async functions for endpoints
# Example: This ORM doesn't support batch inserts over 1000 records
```

## Implementation Blueprint

### Data models and structure

Create the core data models, we ensure type safety and consistency.

```python
Examples:
 - orm models (with i18n fields: title_en, title_fr, etc.)
 - pydantic models (with language field validation)
 - pydantic schemas (with locale-aware serialization)
 - pydantic validators (with multilingual content validation)
 - i18n resource models for translations

# Multilingual Data Patterns:
class MultilingualText(BaseModel):
    en: str  # Required
    fr: str  # Required  
    de: Optional[str] = None
    pt: Optional[str] = None
    es: Optional[str] = None
    zh: Optional[str] = None

class LocalizedModel(BaseModel):
    id: str
    title: MultilingualText
    description: MultilingualText
    locale: str = "en"
    created_at: datetime
```

### Implementation Tasks (ordered by dependencies)

```yaml
Task -2: COMPREHENSIVE visual reference analysis (IF reference provided)
  - CAPTURE: Full screenshots using composition-maestro agent with Playwright
  - DISSECT: Visual hierarchy, layout structure, and component breakdown
  - EXTRACT: Color palette, typography, spacing, and design tokens
  - MEASURE: Dimensions, proportions, and responsive behavior
  - IDENTIFY: CSS frameworks, animation patterns, and interactive elements
  - DOCUMENT: Complete visual analysis report for implementation reference
  - VALIDATE: Reference fidelity requirements and implementation approach

Task -1: ANALYZE template and design system requirements
  - ANALYZE: Existing template structure, components, and patterns
  - INVENTORY: Available components, styles, and assets from template
  - IDENTIFY: Required custom components not provided by template
  - PLAN: Asset generation strategy (images, icons, illustrations)
  - DOCUMENT: Template rules, patterns, and extension guidelines
  - VALIDATE: Template compatibility with project requirements

Task -0.7: SETUP database architecture and authentication system (IF database required)
  - DESIGN: Database schema with core user management tables (users, roles, permissions, sessions)
  - SETUP: Authentication system with email/password + OAuth integration
  - CONFIGURE: User roles hierarchy (admin, manager, user, guest) with proper permissions
  - IMPLEMENT: Cross-platform authentication endpoints and token management
  - CREATE: Database migrations for all user management and application tables
  - SETUP: Security measures (password hashing, rate limiting, audit logging)
  - CONFIGURE: Multi-tenant support and organization structure (if applicable)
  - INTEGRATE: Authentication with all platforms (Next.js, Flutter, Python backend)
  - TEST: Complete authentication flows including registration, login, password reset
  - DOCUMENT: Database schema, API endpoints, and authentication integration guide

Task -0.6: SETUP payment system and e-commerce infrastructure (IF payments required)
  - CONFIGURE: Payment processor integration (Stripe, PayPal, etc.) with API keys
  - DESIGN: E-commerce database schema (products, orders, payments, subscriptions)
  - IMPLEMENT: Payment processing endpoints with webhook handlers
  - SETUP: Shopping cart functionality and session persistence
  - CREATE: Checkout flow with payment validation and error handling
  - CONFIGURE: Payment method support (cards, digital wallets, BNPL)
  - IMPLEMENT: Subscription management and recurring billing logic
  - SETUP: Order management system with fulfillment workflows
  - TEST: Complete payment flows including success, failure, and refund scenarios
  - SECURE: PCI compliance validation and payment data protection

Task -0.5: SETUP animation and typography systems
  - DEFINE: Typography scale, font families, weights, and responsive behavior
  - IMPLEMENT: Animation timing, easing curves, and interaction patterns
  - CREATE: src/styles/typography.py (or CSS/style definitions for frontend)
  - CREATE: src/animations/constants.py (or animation configuration)
  - DOCUMENT: Animation and typography usage guidelines
  - VALIDATE: Typography scales work across all viewport sizes
  - ENSURE: Animation performance meets 60fps standards

Task 0: CREATE src/i18n/locales/ directory and translation files
  - IMPLEMENT: locale.json files for en, fr, de, pt, es, zh
  - CREATE: src/i18n/i18n_service.py for translation management
  - SETUP: Language detection and switching logic
  - FOLLOW pattern: Standard i18n directory structure
  - NAMING: locale codes (en.json, fr.json, etc.)
  - PLACEMENT: Centralized i18n directory in src/

Task 1: CREATE src/models/{domain}_models.py
  - IMPLEMENT: {SpecificModel}Request, {SpecificModel}Response Pydantic models
  - INCLUDE: MultilingualText fields for user-facing content
  - FOLLOW pattern: src/models/existing_model.py (field validation approach)
  - NAMING: CamelCase for classes, snake_case for fields
  - PLACEMENT: Domain-specific model file in src/models/

Task 2: CREATE src/services/{domain}_service.py
  - IMPLEMENT: {Domain}Service class with async methods
  - FOLLOW pattern: src/services/database_service.py (service structure, error handling)
  - NAMING: {Domain}Service class, async def create_*, get_*, update_*, delete_* methods
  - DEPENDENCIES: Import models from Task 1
  - PLACEMENT: Service layer in src/services/

Task 3: CREATE src/tools/{action}_{resource}.py
  - IMPLEMENT: MCP tool wrapper calling service methods
  - FOLLOW pattern: src/tools/existing_tool.py (FastMCP tool structure)
  - NAMING: snake_case file name, descriptive tool function name
  - DEPENDENCIES: Import service from Task 2
  - PLACEMENT: Tool layer in src/tools/

Task 4: MODIFY src/main.py or src/server.py
  - INTEGRATE: Register new tool with MCP server
  - FIND pattern: existing tool registrations
  - ADD: Import and register new tool following existing pattern
  - PRESERVE: Existing tool registrations and server configuration

Task 5: CREATE src/services/tests/test_{domain}_service.py
  - IMPLEMENT: Unit tests for all service methods (happy path, edge cases, error handling)
  - FOLLOW pattern: src/services/tests/test_existing_service.py (fixture usage, assertion patterns)
  - NAMING: test_{method}_{scenario} function naming
  - COVERAGE: All public methods with positive and negative test cases
  - PLACEMENT: Tests alongside the code they test

Task 6: CREATE src/tools/tests/test_{action}_{resource}.py
  - IMPLEMENT: Unit tests for MCP tool functionality
  - FOLLOW pattern: src/tools/tests/test_existing_tool.py (MCP tool testing approach)
  - MOCK: External service dependencies
  - COVERAGE: Tool input validation, success responses, error handling
  - PLACEMENT: Tool tests in src/tools/tests/
```

### Implementation Patterns & Key Details

```python
# Show critical patterns and gotchas - keep concise, focus on non-obvious details

# Example: Service method pattern
async def {domain}_operation(self, request: {Domain}Request) -> {Domain}Response:
    # PATTERN: Input validation first (follow src/services/existing_service.py)
    validated = self.validate_request(request)

    # GOTCHA: [Library-specific constraint or requirement]
    # PATTERN: Error handling approach (reference existing service pattern)
    # CRITICAL: [Non-obvious requirement or configuration detail]

    return {Domain}Response(status="success", data=result)

# Example: MCP tool pattern
@app.tool()
async def {tool_name}({parameters}) -> str:
    # PATTERN: Tool validation and service delegation (see src/tools/existing_tool.py)
    # RETURN: JSON string with standardized response format
```

### Integration Points

```yaml
DATABASE:
  - migration: "Add column 'feature_enabled' to users table"
  - index: "CREATE INDEX idx_feature_lookup ON users(feature_id)"

CONFIG:
  - add to: config/settings.py
  - pattern: "FEATURE_TIMEOUT = int(os.getenv('FEATURE_TIMEOUT', '30'))"

ROUTES:
  - add to: src/api/routes.py
  - pattern: "router.include_router(feature_router, prefix='/feature')"
```

## Validation Loop

### Level 1: Syntax & Style (Immediate Feedback)

```bash
# Run after each file creation - fix before proceeding
ruff check src/{new_files} --fix     # Auto-format and fix linting issues
mypy src/{new_files}                 # Type checking with specific files
ruff format src/{new_files}          # Ensure consistent formatting

# Project-wide validation
ruff check src/ --fix
mypy src/
ruff format src/

# Expected: Zero errors. If errors exist, READ output and fix before proceeding.
```

### Level 2: Unit Tests (Component Validation)

```bash
# Test each component as it's created
uv run pytest src/services/tests/test_{domain}_service.py -v
uv run pytest src/tools/tests/test_{action}_{resource}.py -v

# Database and authentication testing (if applicable)
uv run pytest src/auth/tests/test_authentication.py -v
uv run pytest src/database/tests/test_models.py -v
uv run pytest src/database/tests/test_migrations.py -v

# Full test suite for affected areas
uv run pytest src/services/tests/ -v
uv run pytest src/tools/tests/ -v
uv run pytest src/auth/tests/ -v

# Coverage validation (if coverage tools available)
uv run pytest src/ --cov=src --cov-report=term-missing

# Expected: All tests pass including authentication flows. If failing, debug root cause and fix implementation.
```

### Level 3: Integration Testing (System Validation)

```bash
# Service startup validation
uv run python main.py &
sleep 3  # Allow startup time

# Health check validation
curl -f http://localhost:8000/health || echo "Service health check failed"

# Database and authentication endpoint testing (if applicable)
curl -X POST http://localhost:8000/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"testpass123","first_name":"Test","last_name":"User"}'

curl -X POST http://localhost:8000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"testpass123"}'

# Test protected endpoints with authentication
JWT_TOKEN=$(curl -s -X POST http://localhost:8000/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"testpass123"}' | jq -r '.access_token')

curl -X GET http://localhost:8000/user/profile \
  -H "Authorization: Bearer $JWT_TOKEN"

# Feature-specific endpoint testing
curl -X POST http://localhost:8000/{your_endpoint} \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $JWT_TOKEN" \
  -d '{"test": "data"}' \
  | jq .  # Pretty print JSON response

# MCP server validation (if MCP-based)
# Test MCP tool functionality
echo '{"method": "tools/call", "params": {"name": "{tool_name}", "arguments": {}}}' | \
  uv run python -m src.main

# Database validation (if database integration)
# Verify database schema, connections, migrations
psql $DATABASE_URL -c "SELECT 1;" || echo "Database connection failed"

# Expected: All integrations working, proper responses, no connection errors
```

### Level 4: Creative & Domain-Specific Validation

```bash
# ═══════════════════════════════════════════════════════════
# SKILL-POWERED VALIDATION GATES
# Activate relevant skills from Recommended Skills section
# ═══════════════════════════════════════════════════════════

# --- Design & UI Validation (skills: ui-ux-pro-max, frontend-design) ---
# Verify design avoids anti-patterns: generic purple gradients, over-rounded corners, cookie-cutter SaaS
# Check: hero section designed first, style inherits downward
# Check: monochrome + accent color palette (not rainbow gradients)
# Check: big typography with clear hierarchy (not small uniform text)

# --- Typography Validation (skills: typography, font-pairing-suggester, web-typography) ---
# Verify font pairings follow brand-typography-systems guidelines
# Check: heading/body font contrast, size scale, weight hierarchy
# Check: line-height, letter-spacing, responsive font scaling

# --- Icon System Validation (skills: icon-design, better-icons, svg-icon-generator) ---
# Verify consistent icon system declared upfront (e.g., iconify solar duotone + SVG logos)
# Check: semantic icon selection matches context
# Check: no mixed icon libraries or inconsistent sizing

# --- Web Standards (skills: web-design-guidelines, audit-website, seo-audit) ---
# Accessibility compliance (WCAG 2.1 AA)
# SEO metadata and structure validation
# Performance audit (Core Web Vitals)

# ═══════════════════════════════════════════════════════════
# MCP SERVER & TOOL VALIDATION
# ═══════════════════════════════════════════════════════════

# Context7 MCP (for documentation validation)
context7-mcp --validate-implementation --check-best-practices --framework-compliance

# Playwright MCP (for web interfaces and comprehensive testing)
playwright-mcp --url http://localhost:8000 --test-user-journey --capture-screenshots --responsive-test

# GSAP Master MCP (for professional animation validation)
gsap-performance-test --fps-monitor --memory-usage --accessibility-check

# Replicate MCP (for AI-assisted validation and testing)
replicate-mcp --generate-test-scenarios --validate-edge-cases --performance-analysis

# Unsplash MCP (for asset validation and management)
unsplash-mcp --validate-images --check-licensing --optimize-assets

# Docker MCP (for containerized services)
docker-mcp --build --test --cleanup

# Database MCP (for data operations)
database-mcp --validate-schema --test-queries --check-performance

# ═══════════════════════════════════════════════════════════
# VISUAL REFERENCE VALIDATION (if reference provided)
# ═══════════════════════════════════════════════════════════

# Compare implementation against reference screenshots
python scripts/visual_regression_test.py --reference-screenshots reference/ --current-screenshots current/ --threshold 95
playwright test tests/visual-comparison.spec.js --config=visual-regression.config.js

# Validate responsive behavior matches reference
python scripts/responsive_comparison.py --reference-url "https://reference-site.com" --local-url "http://localhost:8000" --viewports 1920,1366,1024,768,414

# Color palette validation against extracted reference colors
python scripts/validate_color_usage.py --reference-colors "#1234ab,#5678cd" --implementation-css styles/

# Typography comparison validation
python scripts/typography_validation.py --reference-fonts "Inter,Roboto" --check-sizes --check-weights

# Component sizing validation against measurements
python scripts/validate_dimensions.py --reference-measurements reference-measurements.json --current-url "http://localhost:8000"

# Multilingual Validation Commands:
# Test language switching functionality
curl -H "Accept-Language: fr" http://localhost:8000/{endpoint}
curl -H "Accept-Language: en" http://localhost:8000/{endpoint}

# Validate all required languages have translations
python -c "import json; langs=['en','fr']; [print(f'{lang}: OK' if json.load(open(f'src/i18n/locales/{lang}.json')) else f'{lang}: MISSING') for lang in langs]"

# Check for missing translation keys across languages
python scripts/validate_translations.py --check-missing --required-langs en,fr

# Test RTL language support (if implemented)
curl -H "Accept-Language: ar" http://localhost:8000/{endpoint}

# Validate multilingual database content
psql $DATABASE_URL -c "SELECT COUNT(*) FROM content WHERE title_en IS NULL OR title_fr IS NULL;"

# Custom Business Logic Validation
# [Add domain-specific validation commands here]

# Performance Testing (if performance requirements)
ab -n 100 -c 10 http://localhost:8000/{endpoint}

# Security Scanning (if security requirements)
bandit -r src/

# Load Testing (if scalability requirements)
# wrk -t12 -c400 -d30s http://localhost:8000/{endpoint}

# API Documentation Validation (if API endpoints)
# swagger-codegen validate -i openapi.json

# Expected: All creative validations pass, performance meets requirements, all languages working
```

## Final Validation Checklist

### Technical Validation

- [ ] All 4 validation levels completed successfully
- [ ] All tests pass: `uv run pytest src/ -v`
- [ ] No linting errors: `uv run ruff check src/`
- [ ] No type errors: `uv run mypy src/`
- [ ] No formatting issues: `uv run ruff format src/ --check`

### Feature Validation

- [ ] All success criteria from "What" section met
- [ ] Manual testing successful: [specific commands from Level 3]
- [ ] Error cases handled gracefully with proper error messages
- [ ] Integration points work as specified
- [ ] User persona requirements satisfied (if applicable)

### Multilingual Validation

- [ ] English and French translations complete and tested
- [ ] Language switching mechanism working correctly
- [ ] All user-facing text externalized (no hardcoded strings)
- [ ] Database schema supports multilingual content
- [ ] API responses include proper language context
- [ ] Error messages and notifications are translatable
- [ ] Optional languages (DE, PT, ES, ZH) infrastructure ready
- [ ] Translation validation script passes for required languages

### GSAP Animation System Validation (if animations required)

- [ ] **GSAP Performance Standards**:
  - [ ] All animations run at consistent 60fps
  - [ ] Animation bundle size remains under 50kb
  - [ ] First animation triggers within 100ms of page load
  - [ ] Memory cleanup implemented for all timeline instances
  
- [ ] **GSAP Implementation Quality**:
  - [ ] Proper easing functions used (no linear animations)
  - [ ] Animation durations follow UI standards (200-800ms)
  - [ ] Consistent animation language across all components
  - [ ] Timeline sequences properly orchestrated
  
- [ ] **GSAP Accessibility Compliance**:
  - [ ] prefers-reduced-motion media query support implemented
  - [ ] gsap.matchMedia() used for responsive animation behavior
  - [ ] Animation controls (pause/play) available where appropriate
  - [ ] Screen reader compatibility maintained during animations
  
- [ ] **GSAP Cross-Platform Consistency** (if multi-platform project):
  - [ ] Web GSAP timing matches Flutter animation equivalents
  - [ ] Visual animation language consistent across platforms
  - [ ] Touch gesture animations adapted from web hover effects
  - [ ] Brand animation patterns maintained across web/mobile

### Visual Reference Validation (if reference provided)

- [ ] Visual regression tests pass against reference screenshots
- [ ] Responsive behavior matches reference across all viewports
- [ ] Color palette matches extracted reference colors
- [ ] Typography scales and fonts match reference specifications
- [ ] Component dimensions and spacing match measurements
- [ ] Interactive states (hover, focus, active) replicate reference behavior
- [ ] Animation and transition patterns match reference timing with GSAP implementation
- [ ] Performance meets or exceeds reference benchmarks including animation performance

### Code Quality Validation

- [ ] Follows existing codebase patterns and naming conventions
- [ ] File placement matches desired codebase tree structure
- [ ] Anti-patterns avoided (check against Anti-Patterns section)
- [ ] Dependencies properly managed and imported
- [ ] Configuration changes properly integrated

### Documentation & Deployment

- [ ] Code is self-documenting with clear variable/function names
- [ ] Logs are informative but not verbose
- [ ] Environment variables documented if new ones added

---

## MCP Server Integration

### Available MCP Servers for Enhanced Development

**Context7 MCP**: Documentation and Up-to-Date Information
- Use `mcp__context7__resolve-library-id` to find specific libraries and frameworks
- Use `mcp__context7__get-library-docs` to access comprehensive documentation
- Essential for accessing latest API documentation, best practices, and implementation guides

**GSAP Master MCP**: Animation Library Documentation
- Access advanced animation patterns, timing functions, and performance optimization techniques
- Reference motion design principles and complex animation orchestration patterns
- Use for creating smooth, professional animations with optimized performance

**Playwright MCP**: Browser Automation and Visual Validation
- Use `mcp__playwright__browser_navigate` to test live implementations against references
- Use `mcp__playwright__browser_take_screenshot` for visual regression testing
- Essential for responsive testing, user journey validation, and cross-browser compatibility

**Replicate MCP**: AI-Powered Development Assistance
- Generate test scenarios, edge cases, and realistic development data
- Create code snippets and implementation patterns using AI models
- Use for automated testing strategies and performance analysis

**Unsplash MCP**: High-Quality Visual Assets
- Access professional stock photography for development and testing
- Maintain consistent visual quality across implementations
- Ensure proper licensing and attribution for production use

### MCP-Enhanced Implementation Workflow

When implementing features using this PRP:
1. **Documentation Research**: Query Context7 MCP for latest framework documentation and best practices
2. **Visual Validation**: Use Playwright MCP to capture and compare implementation against references
3. **Animation Enhancement**: Reference GSAP Master MCP for animation patterns and performance optimization
4. **AI-Assisted Development**: Leverage Replicate MCP for code generation, testing scenarios, and edge case analysis
5. **Asset Management**: Use Unsplash MCP for consistent, professional visual content throughout development

## Anti-Patterns to Avoid

- ❌ Don't create new patterns when existing ones work
- ❌ Don't skip validation because "it should work"
- ❌ Don't ignore failing tests - fix them
- ❌ Don't use sync functions in async context
- ❌ Don't hardcode values that should be config
- ❌ Don't catch all exceptions - be specific
- ❌ Don't skip MCP server utilization when available - leverage Context7 for documentation, Playwright for validation, and others as appropriate
